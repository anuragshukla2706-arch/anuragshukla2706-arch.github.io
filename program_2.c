#include <stdio.h>
int main()
{ int a,b,add;
printf("enter the value of a");
scanf("%d",&a);
printf("enter the value of b");
scanf("%d",&b);
add=a+b;
printf("addition of a two no is=%d",add);
return 0;
}